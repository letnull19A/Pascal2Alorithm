алг prog1 (арг <__args__>);

var
	a: Integer;
	c: string;
	g: string;

begin
	
	// 1
	WriteLn('как вас зовут ?'); 
	readln(c); 
	
	// 2
	вывод ты любишь играть в шахматы?( Если любишь, введи 1. Если нет, то введи 0); 
	Read(a); 
	
	если a = 1 then
		WriteLn('да') 
	иначе 
		WriteLn('нет'); 
	
	// 3
	вывод у тебя есть хобби?( Если есть, введи 1. Если нет, то введи 0);
	Read(a); 
	
	if a = 1 then 
	begin
		WriteLn('да, я играю в шахматы'); 
		WriteLn('Молодец!')
	end 
	иначе 
		WriteLn('нет,у меня совсем нет свободного времени'); 
	
	// 4
	вывод ты любишь читать книги ?( Если любишь, введи 1. Если нет, то введи 0);
	Read(a); 
	
	if a = 1 then 
	begin
		WriteLn('да');
		WriteLn('Молодец, книги это залог хороших знаний!!')
	end 
	иначе 
	begin
		WriteLn('нет'); 
		вывод Плохо,тебе это срочно нужно исправлять 
	end; 
	
	// 5
	вывод ты хорошо учишься ?( Если да, введи 1. Если нет, то введи 0);
	Read(a); 
	
	if a = 1 then
	begin
		WriteLn('да');
		WriteLn('Молодец, я тобой горжусь!!'); 
	end 
	иначе 
	begin
		WriteLn('нет'); 
		WriteLn('Бездельник,как так можно, надо исправлять'); 
	end; 
	
	// 6
	вывод у тебя есть любимое занятие?( Если любишь, введи 1. Если нет, то введи 0);
	Read(a); 
	
	if a = 1 то
	иначе
		WriteLn('нет');
	
	// 7
	вывод Я вот люблю считать считать криптовалюту; 
	вывод Ой, я же совсем про это забыл! Было приятно пообщаться с тобой. Мне пора;
endвсё